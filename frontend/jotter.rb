::TODO::
    :::to ensure Register page redirects to Login page if successful

    ::catch network errors in all components
    ::apply error boundaries
    ::catch auth errors in all components e.g Profile Form
    ::hide login and register buttons after login is successful
    ::hide logout button but reveal login and register after logout is successful
    ::remove meal_category from profile page and replace it with last_swap_count
    ::put the Avatar caption on profile page in a badge and centre it
    <<<=== Don not forget to first duplicate the profile page ===>>>
    ::convert the Access panel on Profile page to Report panel with \
        dropdown buttons to select report types and also include filter,sort and search options
    ::Form validations
    ::disable department field too